spring:
  datasource:
    password: 'postgres'
    url: 'jdbc:postgresql://localhost:5432/postgres2 '
    username: 'postgres'
  jpa:
    hibernate:
      ddl-auto: create
      show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
